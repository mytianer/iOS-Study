\
    fixMethod('LAStoreManagerVC', 'viewDidLoad', 2, false, function(instance, originInvocation, originArguments){ \
    console.log(instance); \
    console.log(originInvocation); \
    console.log(originArguments); \
    
    runInstanceMethod('LAStoreManagerVC', 'setTitle:', 'mmmmmm'); \
    
    });\
\